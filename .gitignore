# Dependências de Node.js (se instaladas na raiz)
node_modules/

# Arquivos de ambiente
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Saídas de build/compilação (se geradas na raiz)
dist/
build/
.next/
out/

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Arquivos de sistema operacional
.DS_Store
Thumbs.db

# IDEs e editores (globais)
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# VSCode (se não for específico de um subprojeto)
.vscode/
# Exceções para VSCode (se você quiser versionar settings, tasks, etc. globais)
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Arquivos temporários
.temp/
.tmp/

# Dados de runtime
pids/
*.pid
*.seed
*.pid.lock

# Relatórios de diagnóstico
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Vercel (se você usar deploy da raiz)
.vercel/

# TypeScript (se houver arquivos tsbuildinfo na raiz)
*.tsbuildinfo

# Banco de dados SQLite (se você tivesse um na raiz, mas já removemos a referência)
# database.sqlite
